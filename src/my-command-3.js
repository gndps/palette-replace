import { litJsonColor } from './palette-replace'
import command_util from './command_util'

export default function() {
  console.log('running my command 3 - lit json color')
  command_util(litJsonColor)
}
