import { unlitJsonWhite } from './palette-replace'
import command_util from './command_util'

export default function() {
  console.log('running my command 2 - unlit json white')
  command_util(unlitJsonWhite)
}
