import { litJsonWhite } from './palette-replace'
import command_util from './command_util'

export default function() {
  console.log('running my command 1 - lit json white')
  command_util(litJsonWhite)
}
